% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/areal-weights.R
\name{areal_weights}
\alias{areal_weights}
\title{Compute area-weighted intersection matrix between two polygon layers}
\usage{
areal_weights(target_sf, source_sf, target_id = "id", source_id = "id")
}
\arguments{
\item{target_sf}{An \code{sf} object with polygon geometries. The target zones
to aggregate into.}

\item{source_sf}{An \code{sf} object with polygon geometries. The source zones
(e.g., census tracts with interpolated data).}

\item{target_id}{Character. Name of the ID column in \code{target_sf}.}

\item{source_id}{Character. Name of the ID column in \code{source_sf}.}
}
\value{
A numeric matrix [n_target x n_source]. Each column is
standardized so that the weights from all target zones sum to 1 (or 0
if the source zone has no overlap). Row names = target IDs, column
names = source IDs.
}
\description{
Builds a weight matrix that maps values from source polygons (e.g., census
tracts) to target polygons (e.g., custom analysis zones) based on the
fraction of area shared between them.
}
\details{
Requires the \code{sf} package. Geometries are made valid with
\code{\link[sf:valid]{sf::st_make_valid()}} before computing intersections.
}
\seealso{
\code{\link[=areal_interpolate]{areal_interpolate()}} to apply the weights.

Other spatial: 
\code{\link{areal_interpolate}()},
\code{\link{compute_travel_times}()},
\code{\link{download_r5r_data}()}
}
\concept{spatial}
