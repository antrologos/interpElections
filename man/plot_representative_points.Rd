% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/representative-points.R
\name{plot_representative_points}
\alias{plot_representative_points}
\title{Plot representative point diagnostics}
\usage{
plot_representative_points(
  result,
  tracts_sf = NULL,
  pop_raster = NULL,
  tract = NULL,
  osm_roads = NULL,
  ...
)
}
\arguments{
\item{result}{An \code{interpElections_result} object, or an \code{sf} POINT
object returned by \code{\link[=compute_representative_points]{compute_representative_points()}}.}

\item{tracts_sf}{Census tract polygons. Extracted from \code{result} if
available, otherwise required.}

\item{pop_raster}{A \link[terra:SpatRaster-class]{terra::SpatRaster} or \code{NULL}. Extracted from
\code{result} if available. Required for single-tract detail view.}

\item{tract}{Character vector. Tract IDs to plot in detail. If \code{NULL}
(default), shows a summary map of all tracts.}

\item{osm_roads}{An \code{sf} object with road geometries, or \code{NULL}.
Extracted from \code{result} if available.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object (invisibly).
}
\description{
Visualize the population-weighted representative point selection for
one or more census tracts. Shows the population raster, identified
clusters, OSM roads, and the selected departure point.
}
\examples{
\dontrun{
result <- interpolate_election_br(3170701, 2022,
             keep = c("pop_raster", "osm_roads"))

# Summary: all tracts with cluster counts
plot_representative_points(result)

# Detail: single tract showing raster, clusters, roads, point
plot_representative_points(result, tract = "317070105000138")
}

}
