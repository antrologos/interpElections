% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-r5r.R
\name{setup_java}
\alias{setup_java}
\title{Download and configure Java 21 for r5r}
\usage{
setup_java(
  install_dir = file.path(tools::R_user_dir("interpElections", "data"), "java"),
  persist = interactive(),
  verbose = TRUE
)
}
\arguments{
\item{install_dir}{Character. Where to install Java. Default uses
\code{\link[tools:userdir]{tools::R_user_dir()}} so Java lives alongside other R user data.}

\item{persist}{Logical. Write \code{JAVA_HOME} to \verb{~/.Renviron} so it persists
across R sessions. Default: TRUE in interactive sessions.}

\item{verbose}{Logical. Default: TRUE.}
}
\value{
Invisibly, the path to the installed JDK.
}
\description{
Downloads the Adoptium Temurin JDK 21 for the current platform, extracts
it to a local directory, and configures the R session so that r5r can
find it. Optionally persists the configuration to \verb{~/.Renviron}.
}
\details{
The JDK is downloaded from the Eclipse Adoptium project
(\verb{https://adoptium.net}). The archive is extracted to
\code{install_dir/jdk-21} and \code{JAVA_HOME} is set for the current session.

When \code{persist = TRUE}, the function appends (or updates) a \code{JAVA_HOME}
line in \verb{~/.Renviron} so future R sessions find Java automatically.
}
\examples{
\dontrun{
setup_java()
check_r5r()
}

}
