% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reinterpolate.R
\name{reinterpolate}
\alias{reinterpolate}
\title{Re-interpolate with different electoral variables}
\usage{
reinterpolate(
  result,
  what = NULL,
  candidates = NULL,
  parties = NULL,
  cargo = NULL,
  turno = NULL,
  optim = optim_control(),
  keep = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{result}{An \code{interpElections_result} object from
\code{\link[=interpolate_election_br]{interpolate_election_br()}}.}

\item{what}{Character vector. Controls what information is interpolated.
See \code{\link[=interpolate_election_br]{interpolate_election_br()}}. If NULL (default), reuses the original.}

\item{candidates}{Character or numeric vector, or NULL. Filter candidates.
See \code{\link[=interpolate_election_br]{interpolate_election_br()}}.}

\item{parties}{Character vector or NULL. Filter parties.
See \code{\link[=interpolate_election_br]{interpolate_election_br()}}.}

\item{cargo}{Integer, character, or NULL. Override the electoral office.
See \code{\link[=interpolate_election_br]{interpolate_election_br()}}.}

\item{turno}{Integer or NULL. Override the election round (1 or 2).}

\item{optim}{An \code{\link[=optim_control]{optim_control()}} object with optimization parameters.
Default: \code{optim_control()}.}

\item{keep}{Character vector or NULL. Heavy objects to include in result.
See \code{\link[=interpolate_election_br]{interpolate_election_br()}}.}

\item{verbose}{Logical. Print progress messages. Default: TRUE.}

\item{...}{Additional arguments forwarded to \code{\link[=interpolate_election_br]{interpolate_election_br()}}.}
}
\value{
An \code{interpElections_result} object (same structure as the
original, with updated interpolation).
}
\description{
Takes a previous \code{\link[=interpolate_election_br]{interpolate_election_br()}} result and re-runs the
interpolation pipeline using the stored travel time matrix, skipping
the expensive routing step. Useful for switching to different candidates,
parties, \code{what} types, or election rounds without recomputing travel times.
}
\details{
The previous result must contain a \code{time_matrix} (always kept
by default since v0.2).

\strong{Note}: Alpha is re-optimized (which can take minutes). Only the
travel-time computation is skipped.
}
\examples{
\dontrun{
# First run (time_matrix is kept by default)
result <- interpolate_election_br(
  3550308, 2022,
  cargo = "presidente"
)

# Re-interpolate for parties instead of candidates
result2 <- reinterpolate(result, what = "parties")

# Re-interpolate for a different cargo
result3 <- reinterpolate(result, cargo = "governador")

# Re-interpolate for turno 2 (runoff)
result4 <- reinterpolate(result, turno = 2)
}

}
\seealso{
\code{\link[=interpolate_election_br]{interpolate_election_br()}}
}
