% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics-ecological.R
\name{plot_income}
\alias{plot_income}
\title{Plot income-vote ecological correlation}
\usage{
plot_income(
  result,
  variable = NULL,
  census_year = NULL,
  type = c("scatter", "map"),
  ...
)
}
\arguments{
\item{result}{An \code{interpElections_result} object (Brazilian).}

\item{variable}{Variable to correlate with income. Accepts column name,
ballot number, candidate name, or party abbreviation.}

\item{census_year}{Census year for income data. NULL auto-detects from
\code{result$census_year}. Must be 2000, 2010, or 2022.}

\item{type}{Character. \code{"scatter"} (default) or \code{"map"}.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object (invisibly). Prints the plot.
}
\description{
Downloads tract-level household head income via \code{censobr} and
produces a scatter plot or side-by-side map of income vs. vote share.
}
\details{
Income data is downloaded from IBGE via the \code{censobr} package.
No deflation is applied — values are used as-is for within-year
correlation analysis.

Variable sources by census year:
\itemize{
\item \strong{2000}: \code{censobr::read_tracts(2000, "Basico")}, variable \code{VAR06}
\item \strong{2010}: \code{censobr::read_tracts(2010, "ResponsavelRenda")},
computed as \code{V022 / V020}
\item \strong{2022}: \code{censobr::read_tracts(2022, "ResponsavelRenda")},
variable \code{V06004}
}
}
