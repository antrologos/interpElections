% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate.R
\name{idw_interpolate}
\alias{idw_interpolate}
\title{Interpolate source data into target zones using IDW weights}
\usage{
idw_interpolate(time_matrix, alpha, source_matrix, offset = 1)
}
\arguments{
\item{time_matrix}{Numeric matrix [n x m]. Raw travel times.
Rows = target zones, columns = source points.}

\item{alpha}{Numeric vector of length n. Optimal decay parameters,
typically from \code{\link[=optimize_alpha]{optimize_alpha()}}.}

\item{source_matrix}{Numeric matrix or data.frame [m x p]. Data at source
points to be interpolated. Each column is a variable (e.g., candidate
votes, attendance counts).}

\item{offset}{Numeric. Travel time offset. Default: 1.}
}
\value{
Numeric matrix [n x p]. Interpolated values at target zones.
Column names are preserved from \code{source_matrix}.
}
\description{
Applies inverse-distance-weighted interpolation to transfer data from
geolocated source points (e.g., polling locations) into target zones
(e.g., census tracts), using optimized decay parameters.
}
\details{
For each target zone i and variable v:
\deqn{\hat{v}_i = \sum_j W_{ij}^{std} \cdot v_j}

where \eqn{W^{std}} is the column-standardized weight matrix from
\code{\link[=idw_weights]{idw_weights()}}.

This function is the main workhorse for applying the interpolation after
optimal alpha values have been found via \code{\link[=optimize_alpha]{optimize_alpha()}}.
}
\examples{
tt <- matrix(c(2, 5, 3, 4, 6, 2), nrow = 2) # 2 zones x 3 sources
alpha <- c(1, 1.5)
src <- matrix(c(80, 120, 100), nrow = 3)      # 3 sources x 1 variable
idw_interpolate(tt, alpha, src)

}
\seealso{
\code{\link[=optimize_alpha]{optimize_alpha()}} to find optimal alpha values,
\code{\link[=idw_weights]{idw_weights()}} to inspect the weight matrix.

Other IDW core: 
\code{\link{idw_gradient}()},
\code{\link{idw_objective}()},
\code{\link{idw_weights}()},
\code{\link{optimize_alpha}()}
}
\concept{IDW core}
