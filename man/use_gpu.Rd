% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/torch-setup.R
\name{use_gpu}
\alias{use_gpu}
\title{Enable or disable GPU acceleration}
\usage{
use_gpu(enable = TRUE, device = NULL, dtype = "float32")
}
\arguments{
\item{enable}{Logical. \code{TRUE} to enable GPU, \code{FALSE} for CPU only.}

\item{device}{Character or NULL. Torch device: \code{"cuda"}, \code{"mps"}, or
\code{"cpu"}. \code{NULL} = auto-detect.}

\item{dtype}{Character. \code{"float32"} or \code{"float64"}. Default: \code{"float32"}.}
}
\value{
Invisibly returns the previous settings as a list.
}
\description{
Sets package-wide options controlling whether \code{\link[=optimize_alpha]{optimize_alpha()}} uses
GPU-accelerated optimization (PB-SGD) or CPU-only methods.
}
\details{
When GPU is enabled, \code{\link[=optimize_alpha]{optimize_alpha()}} uses torch-based PB-SGD
(per-bracket stochastic gradient descent). This requires the \code{torch}
package to be installed with a working GPU backend (CUDA for NVIDIA,
MPS for Apple Silicon).

The per-call \code{use_gpu} parameter in \code{\link[=optimize_alpha]{optimize_alpha()}} always overrides
the global setting.
}
\examples{
use_gpu(FALSE) # ensure CPU mode

}
\seealso{
\code{\link[=check_torch]{check_torch()}} to diagnose GPU setup,
\code{\link[=setup_torch]{setup_torch()}} to install torch with GPU support.
}
